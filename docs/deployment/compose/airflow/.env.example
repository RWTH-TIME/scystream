PRIVATE_IP=10.0.0.7
DB_HOST=db.internal
AIRFLOW_DB_PASSWORD=change-me
# `getent group docker | cut -d: -f3`
DOCKER_GID=998
# same values on every component
AIRFLOW__CORE__FERNET_KEY=generate-with-cryptography-fernet
AIRFLOW__API_AUTH__JWT_SECRET=change-me-long-random
AIRFLOW__API__SECRET_KEY=change-me-long-random
# admin created on the first start
_AIRFLOW_WWW_USER_USERNAME=admin
_AIRFLOW_WWW_USER_PASSWORD=change-me-long-random
# only for private compute block images
GHCR_USERNAME=
GHCR_TOKEN=
