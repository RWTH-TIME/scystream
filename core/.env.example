# this is an example env file you could use for your local development environment
DEVELOPMENT=true

DATABASE_HOST=localhost
DATABASE_PORT=5432

KEYCLOAK_SERVER_URL="http://locahost:8090"
KEYCLOAK_REALM="main"
KEYCLOAK_CLIENT_SECRET="CS2D4zcxNfLVa7GznHmiuCSsTYX5TaNy"

SUPERSET_HOST="http://localhost:8088"
SUPERSET_PUBLIC_URL="http://localhost:8088"
SUPERSET_KEYCLOAK_CLIENT_ID="superset-service"
SUPERSET_KEYCLOAK_CLIENT_SECRET="superset-service-dev-secret"
SUPERSET_EXPORT_S3_PREFIX="projects"

# How Superset reaches the data postgres (only needed if it differs from the
# DEFAULT_CB_CONFIG_PG_* connection, e.g. when Superset is hosted elsewhere)
# SUPERSET_DATA_SQLALCHEMY_URI="postgresql+psycopg2://postgres:postgres@data-postgres:5432/postgres"
